import ConfigurationScreen from "./ConfigurationScreen";

export default ConfigurationScreen;

//this document is redundant, but it is used for the clarity of import/export statements.
//well, I don't really think so, but I decided to try it, since I'm a newbie to the React Native.